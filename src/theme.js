import { createMuiTheme } from "@material-ui/core/styles";

export default createMuiTheme({
  palette: {
    primary: {
      main: "#0a3d62"
    },
    secondary: {
      main: "#e55039"
    },
    green: {
      main: "#008290"
    },
    red: {
      main: "#e55039"
    }
  },
  heights: {
    navBar: 60
  }
});
